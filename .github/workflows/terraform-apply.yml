name: Terraform Apply

# TODO: Activate this workflow when environments are fully implemented.
# on:
#   push:
#     branches: [ "main" ]

jobs:
  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    # environment: production # Enforces manual approval protection if configured in GitHub
    
    steps:
      # - name: Checkout Code
      #   uses: actions/checkout@v3

      # - name: Setup Terraform
      #   uses: hashicorp/setup-terraform@v2

      # - name: Terraform Init
      #   run: terraform init

      # - name: Terraform Apply
      #   run: terraform apply -auto-approve
      
      - name: Placeholder
        run: echo "Terraform Apply workflow placeholder initialized."
